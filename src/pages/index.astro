---
import Layout from './_layout.astro'

if (!Astro.locals.user || !Astro.locals.session) {
  return Astro.redirect('/user/login')
}
---

<Layout>
  <h1>
    {
      Astro.locals.user
        ? `Hello, ${Astro.locals.user.userName}`
        : 'Not logged in.'
    }
  </h1>
</Layout>
